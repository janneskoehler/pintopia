rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /walls/{wallId} {
      allow read: if true;
      allow write: if true;  // TODO: Füge später entsprechende Authentifizierung hinzu
    }
  }
} 